# CodeGPS Pro × 火星文件管理 - 完整使用指南

## 📖 快速开始指南

### 第一步：启动火星文件服务

双击运行桌面上的：
```
火星文件扩展\启动火星文件服务.bat
```

你会看到：
```
🔴 火星文件管理 HTTP 桥接服务
   工作区：C:\Users\Administrator\Desktop\火星文件扩展
   端口：8765

✅ 服务已启动：http://127.0.0.1:8765/rpc

可用命令:
  tree, stat, read, search, outline, symbols
  apply, commit, rollback, create, delete, move
  diff, history

按 Ctrl+C 停止服务
```

**保持这个窗口打开**，服务需要一直运行。

---

### 第二步：重新加载浏览器扩展

1. 打开 Chrome/Edge 浏览器
2. 访问 `chrome://extensions/`
3. 找到"火星文件管理 Bridge"
4. 点击 **刷新/重新加载** 按钮

---

### 第三步：打开 CodeGPS Pro

在浏览器中打开：
```
C:\Users\Administrator\Desktop\CodeGPS-Pro.html
```

按 F12 打开控制台，应该看到：
```
[FM Integration] 火星文件管理集成已加载
[FM Bridge] 已注入火星文件管理集成脚本
```

---

## 🎯 AI 使用示例

### 示例 1：让 AI 帮你查看项目结构

在 CodeGPS Pro 的 AI 对话框中输入：

```
帮我看看当前目录的文件结构
```

AI 会调用 `fm_tree` 工具返回目录树。

---

### 示例 2：读取特定文件

```
读取 src/index.ts 的前 50 行代码
```

AI 会调用 `fm_read_range` 工具读取文件。

---

### 示例 3：搜索代码

```
搜索所有 TypeScript 文件中包含"getUser"的函数
```

AI 会调用 `fm_search` 工具，参数：
```json
{
  "pattern": "getUser",
  "include": "**/*.ts",
  "regex": false
}
```

---

### 示例 4：修改代码

```
帮我在 src/index.ts 的第 10 行添加一个日志语句
```

AI 会：
1. 先读取文件确认当前内容
2. 生成修改操作
3. 调用 `fm_apply_ops` with `dryRun=true`
4. 显示 diff 预览给你确认
5. 你确认后调用 `fm_commit`

---

## 🔧 手动测试工具

### 测试 fm_tree

在浏览器控制台（F12）输入：

```javascript
window.fmApi.tree('.', 2).then(console.log).catch(console.error)
```

### 测试 fm_read_range

```javascript
window.fmApi.readRange('README.md', 0, 50, 0)
  .then(console.log)
  .catch(console.error)
```

### 测试 fm_search

```javascript
window.fmApi.search('function', { regex: false, maxResults: 10 })
  .then(console.log)
  .catch(console.error)
```

---

## 📋 完整的工具列表

### 查询工具

| 工具 | 说明 | 示例 |
|------|------|------|
| `fmApi.tree(path, depth)` | 查看目录树 | `fmApi.tree('.', 3)` |
| `fmApi.stat(path)` | 文件信息 | `fmApi.stat('src/index.ts')` |
| `fmApi.readRange(path, start, end, context)` | 读取范围 | `fmApi.readRange('src/app.ts', 0, 100, 3)` |
| `fmApi.search(pattern, options)` | 搜索 | `fmApi.search('console.log', {include:'**/*.ts'})` |
| `fmApi.symbols(path)` | 代码符号 | `fmApi.symbols('src/utils.ts')` |
| `fmApi.outline(path)` | Markdown 大纲 | `fmApi.outline('README.md')` |

### 修改工具

| 工具 | 说明 | 示例 |
|------|------|------|
| `fmApi.applyOps(path, version, ops, dryRun)` | 应用编辑 | 见下方示例 |
| `fmApi.commit(transactionId)` | 提交 | `fmApi.commit('tx-abc123')` |
| `fmApi.rollback(transactionId)` | 回滚 | `fmApi.rollback('tx-abc123')` |

---

## 💡 修改代码的完整流程

### 步骤 1：读取文件

```javascript
// 先读取文件，获取当前版本 hash
const stat = await fmApi.stat('src/index.ts');
console.log('当前版本:', stat.version);
```

### 步骤 2：定义修改操作

```javascript
const ops = [{
  id: 'op1',
  type: 'replace_range',
  target: {
    type: 'range',
    startLine: 10,
    endLine: 12,
    expectedOldText: 'const x = 1;'
  },
  newText: 'const x = 2;\nconst y = 3;'
}];
```

### 步骤 3：Dry-run 预览

```javascript
const result = await fmApi.applyOps(
  'src/index.ts',
  stat.version,
  ops,
  true  // dryRun = true
);
console.log('修改预览:', result.diff);
```

### 步骤 4：提交修改

```javascript
await fmApi.commit(result.transactionId);
console.log('✅ 修改已提交');
```

### 步骤 5：如需回滚

```javascript
await fmApi.rollback(result.transactionId);
console.log('↩️ 修改已回滚');
```

---

## ⚠️ 常见问题

### Q: 服务启动失败
**A:** 检查以下几点：
1. Node.js 是否安装：`node --version`
2. 火星文件是否构建：`ls 火星文件/dist/cli/index.js`
3. 端口 8765 是否被占用

### Q: 控制台显示"请求超时"
**A:**
1. 确认服务正在运行
2. 检查防火墙设置
3. 重启浏览器

### Q: AI 不知道如何使用工具
**A:**
1. 重新加载扩展
2. 刷新 CodeGPS Pro 页面
3. 确认控制台有 FM Integration 日志

---

## 🎓 最佳实践

1. **先读取再修改** - 永远不要在不了解代码的情况下直接修改

2. **使用 dryRun** - 提交前一定要预览修改内容

3. **小步提交** - 大量修改时拆分为多个小事务

4. **使用锚点定位** - 优先使用 `replace_anchor` 而非仅用行号

5. **定期备份** - 重要修改前先备份文件

---

## 📚 相关文档

- `README.md` - 扩展安装说明
- `AI 工具使用文档.md` - AI 工具详细说明
- 火星文件管理 `/README.md` - 火星文件管理完整文档

---

祝你使用愉快！🎉
