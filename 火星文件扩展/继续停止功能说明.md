# 继续/停止确认功能 - 使用指南

## 🎯 功能概述

当 AI 在执行复杂任务时，如果达到最大迭代次数（默认 10 次）但任务尚未完成，系统会自动暂停并弹出确认提示，让用户决定下一步操作。

---

## 📋 使用场景

### 场景 1：大型代码分析任务

```
❯ 帮我分析整个项目的代码结构，找出所有潜在问题

[迭代 1/10] AI 查看目录结构
[迭代 2/10] AI 读取主要文件
[迭代 3/10] AI 分析代码逻辑
...
[迭代 10/10] AI 还在继续分析

═══════════════════════════════════════════════════════════
  ⚠️  任务尚未完成，是否继续执行？
═══════════════════════════════════════════════════════════

  已执行迭代：10 次
  当前任务：帮我分析整个项目的代码结构，找出所有潜在问题...

  选择：
    [1] 继续执行（让 AI 继续完成任务）
    [2] 停止执行（查看当前结果）
    [3] 增加迭代次数（设置新的最大值）
    [4] 查看详细历史

  请选择 [1-4]:
```

### 场景 2：批量文件处理

```
❯ 把彩虹文件目录的所有 txt 文件内容提取并总结

[迭代 1/10] AI 搜索所有 txt 文件
[迭代 2/10] AI 读取文件 1
[迭代 3/10] AI 读取文件 2
...
[迭代 10/10] AI 还在处理

═══════════════════════════════════════════════════════════
  ⚠️  任务尚未完成，是否继续执行？
═══════════════════════════════════════════════════════════
```

---

## 🔧 选项说明

### [1] 继续执行
- AI 会继续执行任务，使用新的迭代周期
- 适合任务接近完成时使用
- 不会丢失当前进度

### [2] 停止执行
- 停止当前任务，查看已完成的结果
- 适合需要手动干预或改变策略时
- 可以随时通过历史记录继续

### [3] 增加迭代次数
- 设置新的最大迭代次数（1-50）
- 适合已知任务需要更多步骤时
- 设置后当前周期会使用新限制

### [4] 查看详细历史
- 查看所有对话历史记录
- 帮助了解 AI 已执行的操作
- 查看后可以返回选择

---

## 💡 最佳实践

### 1. 合理设置迭代次数
```
# 小任务（读取 1-2 个文件）：3-5 次
# 中等任务（分析目录）：5-10 次
# 大任务（完整代码分析）：10-20 次
# 超大任务（多文件批处理）：20-50 次
```

### 2. 利用并行调用
AI 可以一次返回多个工具调用，例如：
```json
[
  {"tool": "fm", "command": "read", "args": ["file1.txt"]},
  {"tool": "fm", "command": "read", "args": ["file2.txt"]},
  {"tool": "fm", "command": "read", "args": ["file3.txt"]}
]
```
这样一次迭代就能读取 3 个文件，效率更高。

### 3. 监控迭代进度
系统会显示当前迭代次数：
```
[迭代 3/10] AI 正在思考...
```
如果看到接近 10，可以预期会弹出确认提示。

### 4. 使用完成标志
AI 会在完成任务时说：
- "任务已完成"
- "分析完毕"
- "已完成所有操作"

这时不会弹出确认提示，直接显示结果。

---

## ⚙️ 配置说明

### 修改默认迭代次数

在 `mars-platform.js` 中修改：
```javascript
const CONTEXT_CONFIG = {
  maxMessages: 50,
  compressThreshold: 30,
  keepRecent: 10,
  maxIterations: 10,  // 修改这里的值
};
```

### 系统提示词

已告知 AI：
- 最多有 10 次连续调用机会
- 请在接近限制时尽量完成任务
- 任务完成后明确说明

---

## 📊 流程图

```
用户输入任务
    ↓
AI 分析并返回工具调用
    ↓
执行工具 → 失败？→ 让 AI 处理错误
    ↓                ↓
AI 分析结果 ←───────┘
    ↓
还有工具调用？
    ├─ 是 → 继续迭代（iteration++）
    └─ 否 → 显示回复 → 等待用户输入
            ↓
    iteration >= maxIterations？
            ├─ 否 → 继续循环
            └─ 是 → 检查完成标志
                        ├─ 已完成 → 显示回复
                        └─ 未完成 → 弹出确认提示
                                        ↓
                                    用户选择
                                        ↓
                                    继续/停止
```

---

## 🎯 示例对话

### 完整示例

```
🚀 火星编程 CODE ❯ 分析彩虹文件目录的所有文件，找出问题

[迭代 1/10] AI 正在思考...
  [连续 1] 火星文件：tree . --depth 2
  ✓ 工具执行完成

[迭代 2/10] AI 正在思考...
  [连续 4] 火星文件：read file1.txt
  [连续 4] 火星文件：read file2.txt
  [连续 4] 火星文件：read file3.txt
  [连续 4] 火星文件：read file4.json
  ✓ 工具执行完成

[迭代 3/10] AI 正在思考...

## 📊 分析报告

根据对 4 个文件的分析，发现以下问题：

1. file1.txt - 内容为空
2. file2.txt - 格式正常
3. file3.txt - 包含敏感信息
4. file4.json - JSON 格式错误

任务已完成。

═══════════════════════════════════════════════════════════
  ✅ 任务完成，共执行 3 次迭代
═══════════════════════════════════════════════════════════

🚀 火星编程 CODE ❯
```

---

## 🔍 故障排除

### 问题 1：确认提示不出现

检查 AI 回复是否包含完成标志词。如果是，AI 认为任务已完成。

### 问题 2：无法选择选项

确保输入数字 1-4，或者输入中文（继续/停止/增加/详情）

### 问题 3：迭代次数不够用

使用选项 [3] 增加到更大的值，或者让 AI 并行调用更多工具。

---

## 📝 更新日志

**v1.1.0 - 2026-03-13**
- ✅ 新增继续/停止确认功能
- ✅ 支持动态调整迭代次数
- ✅ 新增任务完成标志检测
- ✅ 优化迭代进度显示
- ✅ 增强 AI 效率提示

---

*火星编程 CODE - 让编程更智能*
