# 🔴 火星编程 CODE - 使用手册

## 📖 产品简介

**火星编程 CODE** 是下一代智能编程平台，集成：
- 🤖 AI 智能对话
- 🔴 火星文件管理
- 🔌 MCP 协议支持
- 🎯 精确代码编辑

---

## 🚀 启动方式

### 方式一：配置工具（首次使用推荐）

```bash
双击运行：配置工具.bat
```

按提示输入：
1. **API Key**（阿里云 DashScope）
2. **选择模型**（1-8 选项）

配置自动保存，下次启动直接使用！

---

### 方式二：双击启动

```bash
双击运行：启动编程平台.bat
```

启动后在平台内输入：
```
/api sk-sp-cec7682a7c994e489716da0ad751ce1e
/model qwen3.5-plus
```

---

### 方式三：命令行启动

```bash
# 设置环境变量
setx AI_API_KEY "sk-sp-cec7682a7c994e489716da0ad751ce1e"
setx AI_MODEL "qwen3.5-plus"
setx AI_PROVIDER "dashscope"

# 启动平台
cd "C:\Users\Administrator\Desktop\火星文件扩展"
node mars-platform.js
```

---

## 💬 界面说明

```
██╗  ██╗██╗   ██╗███╗   ██╗████████╗
╚██╗██╔╝██║   ██║████╗  ██║╚══██╔══╝
 ╚███╔╝ ██║   ██║██╔██╗ ██║   ██║
 ██╔██╗ ██║   ██║██║╚██╗██║   ██║
██╔╝ ██╗╚██████╔╝██║ ╚████║   ██║
╚═╝  ╚═╝ ╚═════╝ ╚═╝  ╚═══╝   ╚═╝
                    下一代智能编程平台
═══════════════════════════════════════

  工作区：C:\Users\Administrator\Desktop
  AI 引擎：已配置
  MCP 服务：0 个配置
  火星文件：就绪

═══════════════════════════════════════
  输入 /help 查看帮助  |  /history 历史记录  |  /delete 删除  |  /quit 退出
  提示：聊天记录自动保存，AI 可以查看历史继续聊天

❯
```

---

## 📋 命令大全

### 基础命令

| 命令 | 说明 |
|------|------|
| `/help` | 显示帮助文档 |
| `/quit` | 退出程序 |
| `/clear [all]` | 清空对话（加 all 删除所有历史） |
| `/history` | 查看历史记录 |
| `/delete [序号]` | 删除指定消息 |
| `/summarize` | 总结历史对话 |
| `/config` | 显示配置信息 |
| `/api <key>` | 设置 AI API 密钥 |
| `/model <模型>` | 切换 AI 模型 |
| `/status` | 显示系统状态 |
| `/mcp ...` | MCP 管理命令 |

### MCP 管理命令

| 命令 | 说明 |
|------|------|
| `/mcp list` | 列出所有 MCP 服务器 |
| `/mcp add <名> <命令>` | 添加 MCP 服务器 |
| `/mcp remove <名>` | 删除 MCP 服务器 |
| `/mcp enable <名>` | 启用服务器 |
| `/mcp disable <名>` | 禁用服务器 |
| `/mcp test <名>` | 测试连接 |

---

## 💡 使用示例

### AI 对话

```
❯ 帮我查看 src 目录结构

  AI 正在思考...
  正在执行：fm tree src --depth 2

  AI 正在分析结果...

src 目录结构如下：
├── cli/
│   └── index.ts
├── fm-core/
│   ├── engine.ts
│   ├── types.ts
│   └── ...
└── openclaw-adapter/
```

### 查看历史记录

```
❯ /history

═══════════════════════════════════════════════════════════════
  历史记录
═══════════════════════════════════════════════════════════════

  共 10 条对话，AI 可以查看和分析这些记录

  示例命令:
    查看之前的聊天内容
    继续我们刚才讨论的话题
    总结一下我们都聊了什么
    删除最后一条消息：/delete last
    删除第 1 条消息：/delete 1
    清空所有历史：/clear all

  1. [你] 帮我查看 src 目录结构...
  2. [AI] src 目录结构如下：...
  3. [你] 读取 src/index.ts 的前 50 行...
  4. [AI] 文件内容：...
```

### 让 AI 回忆之前的聊天

```
❯ 继续我们刚才讨论的话题

  AI 正在思考...

  根据历史记录，我们之前讨论了 src 目录结构和 index.ts 文件。
  你当时想要分析这个文件的代码结构...
```

### 总结历史对话

```
❯ /summarize

  AI 正在总结历史记录...

本次对话主要讨论了以下内容：
1. 查看了 src 目录结构
2. 分析了 index.ts 文件的入口代码
3. 讨论了函数重构方案
```

### 删除聊天记录

```
❯ /delete last
  已删除最后一条消息

❯ /delete 1
  已删除第 1 条消息

❯ /clear all
  所有历史记录已删除
```

### 读取文件

```
❯ 读取 src/index.ts 的前 50 行

  AI 正在思考...
  正在执行：fm read src/index.ts --start 0 --end 50

  AI 正在分析结果...

文件内容：
// src/index.ts
// 火星文件管理 CLI 入口
...
```

### 搜索代码

```
❯ 搜索所有 TypeScript 文件中的 getUser 函数

  AI 正在思考...
  正在执行：fm search getUser --include **/*.ts

  AI 正在分析结果...

找到 3 处匹配：
1. src/user.ts:25 - function getUser()
2. src/api.ts:108 - const getUser = async () =>
3. tests/user.test.ts:15 - describe('getUser')
```

### MCP 调用

```
❯ 读取 /tmp/config.json 文件

  AI 正在思考...
  MCP 服务：filesystem.read_file()

  AI 正在分析结果...

文件内容：
{
  "name": "test",
  "version": "1.0.0"
}
```

---

## 🔧 配置说明

### 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `AI_API_KEY` | AI API 密钥 | - |
| `AI_PROVIDER` | API 提供商 | `dashscope` |
| `AI_MODEL` | 模型名称 | `qwen3.5-plus` |
| `AI_BASE_URL` | API 地址 | `https://coding.dashscope.aliyuncs.com/v1` |
| `FM_WORKSPACE` | 工作区路径 | 火星文件目录 |

### 设置 API Key

**方式一：使用配置工具（推荐）**

```bash
双击运行：配置工具.bat
```

按提示输入 API Key 并选择模型，配置自动保存。

**方式二：平台内设置（当前会话）**

```
/api sk-sp-cec7682a7c994e489716da0ad751ce1e
/model qwen3.5-plus
```

**方式三：环境变量（永久设置）**

```bash
setx AI_API_KEY "sk-sp-your-api-key"
setx AI_MODEL "qwen3.5-plus"
setx AI_PROVIDER "dashscope"
```

### 支持的 AI 模型

平台支持 8 种主流大模型：

| 模型 ID | 名称 | 提供商 |
|--------|------|--------|
| `qwen3.5-plus` | Qwen 3.5 Plus ⭐ | 通义千问 |
| `qwen3-max-2026-01-23` | Qwen 3 Max | 通义千问 |
| `qwen3-coder-next` | Qwen 3 Coder Next | 通义千问 |
| `qwen3-coder-plus` | Qwen 3 Coder Plus | 通义千问 |
| `slm-5` | SLM 5 | 通义千问 |
| `glm-4.7` | GLM 4.7 | 智谱 AI |
| `kimi-k2.5` | Kimi K2.5 | 月之暗面 |
| `MiniMax-M2.5` | MiniMax M2.5 | MiniMax |

**切换模型：**
```
/model <模型 ID>
```

---

## 🔌 MCP 服务器配置

### 常用 MCP 服务器

**文件系统：**
```
/mcp add filesystem npx -y @modelcontextprotocol/server-filesystem C:\Users\Administrator\Desktop
```

**GitHub：**
```
/mcp add github npx -y @modelcontextprotocol/server-github
```
需要设置：`GITHUB_TOKEN=your-token`

**SQLite：**
```
/mcp add sqlite npx -y @modelcontextprotocol/server-sqlite
```

**Git：**
```
/mcp add git npx -y @modelcontextprotocol/server-git
```

---

## ⚠️ 常见问题

### 问题 1：未配置 API Key

```
⚠️ 未配置 AI API Key
```

**解决：** 输入 `/api your-api-key`

### 问题 2：火星文件 CLI 不存在

```
❌ 火星文件 CLI 不存在
```

**解决：**
```bash
cd 火星文件
npm install
npm run build
```

### 问题 3：MCP 服务器不存在

```
❌ MCP 服务器 "xxx" 不存在
```

**解决：** 使用 `/mcp list` 查看已配置的服务器

---

## 📊 系统状态

使用 `/status` 查看：

```
═══════════════════════════════════════
  系统状态
═══════════════════════════════════════

  ✅ 火星文件 CLI: 正常
  ✅ AI API Key: 正常
  ✅ MCP 配置: 正常
```

---

## 🎯 最佳实践

1. **首次使用** - 运行 `配置工具.bat` 一键设置 API 和模型
2. **查看帮助** - 使用 `/help` 熟悉命令
3. **小步测试** - 先简单请求，再复杂操作
4. **使用 MCP** - 添加常用 MCP 服务器扩展功能
5. **定期清理** - 使用 `/clear` 清空对话历史
6. **切换模型** - 根据需求选择不同模型
7. **连续执行** - AI 可以自动连续调用多个工具完成复杂任务
8. **历史记录** - 使用 `/history` 查看历史，AI 可以回忆之前的对话

---

## 🔥 高级功能

### 连续工具调用

AI 可以自动连续调用多个工具完成复杂任务。例如：

```
❯ 帮我查看彩虹文件目录并读取里面的文件

  AI 正在思考...
  [1/2] 正在执行：火星文件：tree 彩虹文件
  [2/2] 正在执行：火星文件：read 彩虹文件/新建文本文档 1.txt

  AI 正在分析结果...

彩虹文件目录包含 2 个文件：
1. 新建文本文档 1.txt
2. 新建文本文档 (2).txt

已读取第一个文件内容...
```

### AI 管理历史记录

```
❯ 继续刚才的话题

  AI 正在思考...

  根据历史记录，我们之前讨论了...

❯ /summarize

  AI 正在总结历史记录...

  本次对话主要讨论了以下内容：
  1. ...
  2. ...

❯ /delete last
  已删除最后一条消息

❯ /clear all
  所有历史记录已删除
```

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `MCP 使用指南.md` | MCP 详细文档 |
| `命令行编程平台 - 使用指南.md` | 快速入门指南 |
| `README.md` | 扩展说明 |

---

## 🎉 开始使用

```bash
# 双击启动
启动编程平台.bat

# 或命令行
node mars-platform.js
```

**祝你使用愉快！** 🚀

---

*火星编程 CODE - 让编程更智能*
