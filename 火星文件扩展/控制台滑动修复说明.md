# 控制台滑动问题修复 - 完整说明

## 🔴 问题描述

**用户反馈**：命令行窗口如果聊天内容多，想滑动窗口看之前的聊天内容根本滑不动，看不见之前的内容

---

## ✅ 解决方案

提供 **4 种方式** 查看历史聊天内容：

### 方式 1：聊天日志文件（⭐ 推荐）

所有聊天内容自动保存到 `chat-log.txt` 文件

**优点**：
- ✅ 可以用任何文本编辑器打开
- ✅ 支持搜索、复制、导出
- ✅ 可以查看任意历史内容
- ✅ 不受控制台窗口限制
- ✅ 自动轮转，不占用过多空间

**使用方法**：
```bash
# 方法 A：使用 /log 命令
❯ /log           # 查看最近 50 行
❯ /log 100       # 查看最近 100 行
❯ /log 500       # 查看最近 500 行

# 方法 B：直接打开文件
用记事本打开：C:\Users\Administrator\Desktop\火星文件扩展\chat-log.txt
```

**日志格式**：
```
[2026-03-13-03-56-45] 🚀 火星编程 CODE ❯ 帮我查看 src 目录结构
[2026-03-13-03-56-47]   AI 正在思考...
[2026-03-13-03-56-48]   正在执行：fm tree src --depth 2
[2026-03-13-03-56-50]   ✓ 工具执行完成
```

---

### 方式 2：滑动控制台窗口

启动平台后，控制台自动设置缓冲区大小：
- **宽度**：120 列
- **高度**：9999 行

**滑动方法**：
1. 使用鼠标滚轮
2. 使用窗口右侧滚动条
3. 使用键盘 PageUp / PageDown

**如果无法滑动**，手动设置：
```bash
❯ /setconmode
```

或者手动操作：
1. 右键点击控制台标题栏
2. 选择"属性"
3. 切换到"布局"选项卡
4. 增加"屏幕缓冲区大小"的高度到 9999

---

### 方式 3：AI 历史记录

查看 AI 记忆中的历史对话：

```bash
❯ /history
```

显示内容：
- 对话摘要
- 消息数量
- 可以继续之前的话题

---

### 方式 4：清空日志（需要时）

```bash
❯ /clearlog          # 清空聊天日志
```

---

## 📊 功能对比

| 方式 | 优点 | 缺点 | 推荐场景 |
|------|------|------|---------|
| 日志文件 | 完整、可搜索、可编辑 | 需要打开文件 | ⭐ 查看所有历史 |
| 滑动窗口 | 快速、方便 | 受窗口大小限制 | 查看最近内容 |
| AI 记忆 | 智能总结 | 只显示摘要 | 了解对话大纲 |
| 清空日志 | 释放空间 | 丢失历史 | 日志太大时 |

---

## 🎯 使用建议

### 日常使用
```bash
# 开始新任务前，查看一下之前的日志
❯ /log 20

# 或者有重要内容时，直接打开文件搜索
用记事本打开 chat-log.txt
```

### 长时间对话
```bash
# 如果控制台无法滑动
❯ /setconmode

# 然后使用鼠标滚轮或滚动条查看
```

### 查找特定内容
```bash
# 打开日志文件，使用 Ctrl+F 搜索
chat-log.txt
```

---

## ⚙️ 技术细节

### 日志配置
```javascript
const LOG_CONFIG = {
  enabled: true,              // 是否启用日志
  maxFileSize: 1024 * 1024,   // 最大文件大小 (1MB)
  backupCount: 5,             // 保留的备份数量
};
```

### 日志轮转
当 `chat-log.txt` 超过 1MB 时：
```
chat-log.txt           → chat-log.txt.1.txt
chat-log.txt.1.txt     → chat-log.txt.2.txt
...
chat-log.txt.4.txt     → chat-log.txt.5.txt
chat-log.txt.5.txt     → 删除（最旧）
```

### 控制台缓冲区
```bash
# 使用 mode 命令设置
mode con cols=120 lines=9999

# 或使用 PowerShell
$Host.UI.RawUI.BufferSize = New-Object Management.Automation.Host.Size 120,9999
```

---

## 📝 命令列表

| 命令 | 说明 | 示例 |
|------|------|------|
| `/log` | 查看聊天日志 | `/log` |
| `/log <行数>` | 查看指定行数 | `/log 100` |
| `/clearlog` | 清空日志 | `/clearlog` |
| `/setconmode` | 设置缓冲区 | `/setconmode` |
| `/history` | AI 历史记忆 | `/history` |

---

## 🔍 故障排除

### 问题 1：日志文件不存在
**原因**：还没有聊天内容
**解决**：开始对话后自动创建

### 问题 2：无法滑动窗口
**解决**：
```bash
❯ /setconmode
```

### 问题 3：日志文件太大
**解决**：
```bash
❯ /clearlog
```

### 问题 4：日志内容乱码
**原因**：ANSI 颜色码未完全过滤
**解决**：使用支持 UTF-8 的编辑器（如 Notepad++、VS Code）

---

## 🎉 更新内容

**v1.2.0 - 2026-03-13**

### 新增功能
- ✅ 聊天日志自动保存
- ✅ 日志轮转机制
- ✅ 控制台缓冲区自动设置
- ✅ `/log` 命令
- ✅ `/clearlog` 命令
- ✅ `/setconmode` 命令

### 改进内容
- ✅ 输出函数增加日志记录
- ✅ 欢迎界面显示日志状态
- ✅ 帮助信息增加日志命令
- ✅ 去除 ANSI 颜色码（干净文本）

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `聊天日志功能说明.md` | 详细使用指南 |
| `继续停止功能说明.md` | 继续/停止确认功能 |
| `最终修复总结.md` | 完整修复总结 |

---

## 🎯 最佳实践

1. **定期查看日志** - 使用 `/log 20` 快速查看
2. **重要内容保存** - 日志文件自动保存所有内容
3. **搜索历史** - 用文本编辑器打开日志文件搜索
4. **管理大小** - 日志超过 1MB 自动轮转

---

*火星编程 CODE - 让编程更智能*
*修复完成时间：2026-03-13*
